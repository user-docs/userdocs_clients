{
  "name": "userdocs_clients",
  "version": "0.0.10",
  "private": true,
  "workspaces": [ 
    "packages/*" 
  ],
  "scripts": {
    "docs": "doctoc --title '**Table of content**' README.md",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "publish": "lerna publish",
    "test": "lerna run test",
    "start": "lerna run start",
    "lint": "eslint --ext js,ts ."
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "~4.2.3"
  }
}
