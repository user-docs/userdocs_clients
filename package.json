{
  "name": "userdocs_clients",
  "private": true,
  "workspaces": [ 
    "packages/*" 
  ],
  "scripts": {
    "docs": "doctoc --title '**Table of content**' README.md",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "publish": "lerna publish",
    "compile": "lerna run compile",
    "dist": "lerna run dist",
    "test": "lerna run test",
    "start_desktop": "lerna run compile && lerna run start_desktop --stream",
    "lint": "eslint --ext js,ts ."
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "~4.2.3"
  }
}
